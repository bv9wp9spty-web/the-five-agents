---
title: .env.example
description: תבנית המפתחות לשיתוף - בלי ערכים אמיתיים
type: reference
tags: [file-doc, env, template]
file_path: .env.example
owner: ראובן
created: 2026-05-13
updated: 2026-05-13
---

# .env.example

**נתיב:** `.env.example`
**בעלים:** [[ראובן]]
**סוג:** תבנית של קובץ הסביבה. ב-Git, ללא ערכים אמיתיים.

## מה הקובץ עושה

מציג בפני כל מי שמשכפל את הפרויקט אילו משתני סביבה נדרשים. הוא קובץ הוראות, לא קובץ סודות.

## מבנה ותוכן

קבוצות המפתחות הפעילות, עם הערות בעברית שמסבירות לאיזה סוכן כל מפתח שייך:

| משתנה | תיאור | סוכן בעל הקובץ |
| --- | --- | --- |
| `ANTHROPIC_API_KEY` | Claude API לכל הסוכנים | כל הצוות |
| `OPENAI_API_KEY` | יצירת תמונות ומודלים נוספים | [[יובל]] |
| `DEFAULT_MODEL` | מודל ברירת מחדל (כרגע `claude-opus-4-7`) | הגדרה כללית |
| `LOG_LEVEL` | רמת לוגים (כרגע `info`) | הגדרה כללית |

[[חן]] משתמשת ב-`WebSearch`/`WebFetch` הפנימיים של Claude Code, ולא ב-API חיצוני בתשלום. אין צורך במפתחות נוספים עבורה. (הגרסה הראשונית של הקובץ כללה `TAVILY_API_KEY` ו-`PERPLEXITY_API_KEY` "לשימוש של חן" — הוסרו ב-2026-05-13 עם כתיבת קובץ ההגדרה של חן.)

## למי הוא משויך

- [[ראובן]] — מתחזק את הקובץ ומעדכן כשמוסיפים מפתחות חדשים.
- [[יעל]], [[יובל]] — כל אחד מהם זקוק לסט מפתחות אחר. הקובץ הוא **המקור הרשמי** של ההחלטה איזה סוכן צורך איזה API. ([[חן]] לא צורכת API חיצוני בעיצוב הנוכחי.)

## קבצים קשורים

- [[env]] — הקובץ האמיתי עם הערכים.
- [[gitignore]] — מציין שרק `.env` (לא `.env.example`) מוחרג מ-Git.
- [[יובל]], [[חן]] — מצוטטים מפורשות בהערות הקובץ.
